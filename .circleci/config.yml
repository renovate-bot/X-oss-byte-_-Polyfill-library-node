version: 2.1

jobs:
  lint:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm ci
      - run: npm run lint
  tests:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm ci
      - run: npm run test
  publish_to_npm:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm ci

workflows:
  test:
    jobs:
      - lint
      - tests
      - publish_to_npm:
          filters:
            branches:
              only: master
          requires:
            - lint
            - tests
